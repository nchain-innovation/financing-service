# System requirements

System requirements for the Financing Service Rust implementation (v4.3.0), with verification methods for each requirement.

For API behaviour see [SupportedEndpoints.md](SupportedEndpoints.md). For configuration see [Configuration.md](Configuration.md). For implementation status see [Project.md](Project.md).

## Scope

These requirements apply to the Financing Service (FS) as delivered in this repository: a REST API that creates Bitcoin SV funding transaction outpoints for configured client wallets.

**In scope:** REST API, wallet/funding logic, blockchain interfaces (WhatsOnChain, UaaS, node RPC, test), the optional mapi-lite transaction broadcaster, configuration, authentication, rate limiting, optional OpenTelemetry trace export, Docker deployment, and CI quality gates.

**Out of scope:** HTTPS termination (deployment layer), HD (BIP-32) wallets, client-side funding-transaction caching, trusted-proxy rate-limit key extraction, and rollback of on-chain transactions after partial `multiple_tx` failure.

## Verification

Every requirement below has at least one automated test. Run the full suite with:

```bash
cargo test
```

Requirement IDs appear in test function names (for example `sr_fund_008_*`) or are covered by existing tests cited in the **Verification** column.

## Functional requirements — REST API

| ID | Requirement | Priority | Verification |
|----|-------------|----------|--------------|
| SR-FUNC-001 | The service SHALL expose `GET /` returning plain-text service identifier `Financing Service REST API`. | Must | **AT:** `rest_api::tests::test_index` |
| SR-FUNC-002 | The service SHALL expose `GET /health` returning HTTP 200 and JSON `{"status":"ok"}` whenever the process is running, whatever else is configured. | Must | **AT:** `rest_api::tests::test_health`, `sr_bchn_012_health_stays_up_when_mapi_lite_is_unreachable`, `responses::tests::health_response_without_mapi_lite_is_exactly_status_ok` |
| SR-FUNC-003 | The service SHALL expose `GET /status` returning version, blockchain connection status, and last update time. | Must | **AT:** `rest_api::tests::test_status` |
| SR-FUNC-004 | The service SHALL expose `POST /fund` accepting JSON (`client_id`, `satoshi`, `no_of_outpoints`, `multiple_tx`, `locking_script`) and returning funding `outpoints` and `txs` on success. | Must | **AT:** `rest_api::tests::test_fund_success`, `test_fund_multiple_tx_success` |
| SR-FUNC-005 | The service SHALL expose `GET /client/{client_id}/balance` returning confirmed and unconfirmed satoshi balances. | Must | **AT:** `rest_api::tests::test_balance_success` |
| SR-FUNC-006 | The service SHALL expose `GET /client/{client_id}/address` returning the client's funding address. | Must | **AT:** `rest_api::tests::test_address_success` |
| SR-FUNC-007 | The service SHALL expose `POST /client` to add a client at runtime and persist it to the dynamic config file. | Must | **AT:** `rest_api::tests::sr_func_007_add_client_persists_to_dynamic_config_file`, `test_add_client_success`, `test_add_client_with_wif_env` |
| SR-FUNC-008 | The service SHALL expose `DELETE /client/{client_id}` to remove a dynamically added client. | Must | **AT:** `rest_api::tests::test_delete_client_success` |
| SR-FUNC-009 | Unknown `client_id` SHALL return a descriptive JSON error with code `unknown_client`: HTTP 404 where the id is a path segment, HTTP 400 where it is a request body field. | Must | **AT:** `rest_api::tests::test_balance_unknown_client`, `test_address_unknown_client`, `test_fund_unknown_client`, `test_delete_client_unknown` |
| SR-FUNC-010 | Invalid fund request parameters (`satoshi`, `no_of_outpoints`, `locking_script`, `locking_scripts`) SHALL return HTTP 400 with code `invalid_request` and a descriptive JSON error. | Must | **AT:** `rest_api::tests::test_fund_invalid_satoshi`, `test_fund_invalid_no_of_outpoints`, `test_fund_invalid_locking_script` |
| SR-FUNC-011 | Successful JSON responses SHALL use HTTP 200. Error JSON responses SHALL use the status mapped to their `code`, such that 5xx indicates a failure worth retrying unchanged and 4xx one requiring the caller or an operator to act. | Must | **AT:** `rest_api::tests::sr_func_011_success_and_error_responses_use_expected_status_codes`, `responses::tests::each_error_code_maps_to_its_documented_status`, `responses::tests::retryable_codes_are_5xx_and_client_errors_are_4xx` |

## Functional requirements — funding

| ID | Requirement | Priority | Verification |
|----|-------------|----------|--------------|
| SR-FUND-001 | The service SHALL sign funding transactions using only the configured client WIF; it SHALL NOT access the requesting application's private keys. | Must | **AT:** `client::tests::sr_fund_001_funding_tx_uses_supplied_locking_script` |
| SR-FUND-002 | The service SHALL reject funding when total wallet balance is insufficient for requested outputs and fees. | Must | **AT:** `rest_api::tests::test_fund_insufficient_balance`, `client::tests::test_funding_balance_error_insufficient_total` |
| SR-FUND-003 | The service SHALL combine multiple UTXOs in one transaction when no single UTXO covers the required amount including fees. | Must | **AT:** `rest_api::tests::test_fund_consolidates_multiple_utxos`, `client::tests::test_create_funding_tx_consolidates_multiple_utxos`, `test_funding_balance_error_consolidates_multiple_utxos` |
| SR-FUND-004 | When `multiple_tx` is true and `no_of_outpoints` > 1, the service SHALL create and broadcast separate transactions per outpoint. | Must | **AT:** `service::tests::fund_with_multiple_transactions_broadcasts_each_tx_separately`, `rest_api::tests::test_fund_multiple_tx_success` |
| SR-FUND-005 | Before building a funding transaction, the service SHALL refresh the client's UTXO state from the blockchain, unless that state is younger than `service.chain_state_max_age_seconds` (SR-FUND-019). The service's own spends are applied to the cache as they happen, so the refresh guards only against an input spent from outside the service. | Must | **AT:** `rest_api::tests::sr_fund_005_fund_endpoint_refreshes_stale_utxo_cache`, `service::tests::refresh_client_chain_state_restores_stale_balance_and_utxo_cache` |
| SR-FUND-006 | On `GET /client/{client_id}/balance`, the service SHALL refresh balance and UTXO state from the blockchain before responding, unless that state is younger than `service.chain_state_max_age_seconds` (SR-FUND-019). | Must | **AT:** `rest_api::tests::sr_fund_006_balance_endpoint_refreshes_stale_utxo_cache`, `service::tests::refresh_client_chain_state_restores_stale_balance_and_utxo_cache` |
| SR-FUND-007 | In `multiple_tx` mode, if a later broadcast fails after earlier successes, the service SHALL return HTTP 422 with code `partial_broadcast` and `description`, and include `outpoints` and `txs` for successfully broadcast transactions when available. This is the one error that retains HTTP 422, because the caller must read the body to learn which transactions reached the chain. | Must | **AT:** `service::tests::partial_broadcast_error_lists_successful_txids`, `responses::tests::partial_funding_error_response_includes_successful_transactions` |
| SR-FUND-008 | After partial `multiple_tx` failure, the service SHALL resync UTXO state from the blockchain. | Must | **AT:** `service::tests::sr_fund_008_partial_multiple_tx_failure_resyncs_chain_state` |
| SR-FUND-009 | Concurrent fund requests for different clients SHALL NOT block each other. | Must | **AT:** `service::tests::concurrent_fund_requests_for_different_clients_do_not_block` |
| SR-FUND-010 | Concurrent fund requests for the same client SHALL use read-only planning under a shared lock and commit UTXO cache updates only after successful broadcast. | Must | **AT:** `client::tests::sr_fund_010_plan_funding_tx_leaves_utxo_cache_unchanged_until_commit`, `service::tests::concurrent_fund_requests_for_same_client_do_not_block` |
| SR-FUND-011 | A broadcast whose outcome the service cannot determine -- the mapi-lite submit deadline expired, or the answer could not be read -- SHALL be reported as code `broadcast_outcome_unknown` with HTTP 504, distinct from `broadcast_failed` (HTTP 502), which means the transaction was not taken. A refused connection SHALL be classified as `broadcast_failed`, since nothing was delivered. The description SHALL tell the caller not to retry with a new `idempotency_key`, and the idempotency record SHALL NOT be released, so a retry carrying the original key is refused rather than funded again. | Must | **AT:** `rest_api::tests::sr_fund_011_fund_reports_an_unknown_outcome_as_504_not_502`, `sr_fund_011_an_unknown_outcome_does_not_release_the_idempotency_key`, `broadcaster::mapi::tests::mapi_broadcaster_submit_is_bounded_by_the_total_deadline`, `mapi_broadcaster_refuses_a_result_count_other_than_one`, `mapi_broadcaster_refuses_a_txid_that_is_not_the_transactions_own`, `mapi_broadcaster_rejects_a_tampered_envelope`, `sr_fund_011_an_unreachable_mapi_lite_is_a_failure_not_an_unknown_outcome` |
| SR-FUND-012 | When a broadcast outcome is unknown, the service SHALL treat the transaction's inputs as spent: they SHALL be removed from the client's UTXO cache and reserved, so no later refresh offers them to another funding request while the transaction may be on the network. The reservation SHALL be released when the chain stops reporting the outpoint as unspent, and SHALL expire after a bounded period so funds are not stranded if the transaction never landed. The unconfirmed change output SHALL NOT be added to the cache for an unknown outcome, and a broadcast known to have failed SHALL reserve nothing. A broadcast that succeeded SHALL reserve its inputs as well (SR-FUND-015). | Must | **AT:** `service::tests::sr_fund_012_an_uncertain_broadcast_does_not_leave_its_input_spendable`, `sr_fund_012_the_reservation_survives_the_refresh_on_the_error_path`, `sr_fund_012_a_failed_broadcast_leaves_its_input_spendable`, `client::tests::sr_fund_012_a_refresh_does_not_resurrect_a_reserved_outpoint`, `sr_fund_012_a_reservation_is_released_once_the_chain_agrees_it_is_spent`, `sr_fund_012_a_reservation_expires_so_funds_return_if_nothing_landed`, `sr_fund_012_a_reserved_outpoint_is_off_the_balance_too`, `sr_fund_012_an_uncertain_commit_does_not_add_the_change_output`, `sr_fund_012_a_successful_broadcast_reserves_its_inputs` |
| SR-FUND-013 | The service SHALL make every submit attempt its configuration asks for: `max_retries + 1` attempts, each bounded by `timeout_seconds` and the sequence bounded by `total_timeout_seconds`, with linear back-off between them. Retries SHALL be driven by the service rather than by `uls-client`, so each attempt's outcome stays distinguishable (SR-FUND-011). A retry after an attempt that timed out SHALL be able to settle the outcome, since mapi-lite reports a transaction it already holds as a success. Once any attempt may have been delivered, a later transport failure SHALL NOT downgrade the result to `broadcast_failed`; a deadline consumed by back-off alone, with no attempt left in flight, SHALL remain `broadcast_failed`. | Must | **AT:** `broadcaster::mapi::tests::sr_fund_013_every_configured_attempt_is_actually_made`, `sr_fund_013_the_deadline_still_cuts_a_budget_that_cannot_fit`, `sr_fund_013_a_retry_resolves_an_attempt_that_timed_out`, `sr_fund_013_a_timeout_then_a_failure_is_still_an_unknown_outcome`, `sr_fund_013_backoff_exhausting_the_deadline_is_a_failure_not_unknown`, `mapi_broadcaster_retries_a_server_error_then_gives_up` |
| SR-FUND-014 | A broadcast the upstream refuses SHALL be reported by whether the upstream said a resubmission could succeed. A refusal it calls final SHALL use code `broadcast_rejected` with HTTP 409, since nothing was spent and retrying cannot help; a refusal it calls retryable, and an unreachable upstream, SHALL keep `broadcast_failed` with HTTP 502 and its "retry may succeed" advice. The upstream's reason and its `retryable` verdict SHALL appear in the service log and SHALL NOT appear in the response body. A final refusal SHALL release the request's idempotency record, since the upstream answering is proof nothing was spent. | Must | **AT:** `rest_api::tests::sr_fund_014_a_permanent_rejection_is_not_reported_as_retryable`, `sr_fund_014_a_retryable_rejection_stays_broadcast_failed`, `sr_fund_014_a_permanent_rejection_releases_the_idempotency_key`, `sr_fund_014_an_unknown_outcome_still_holds_the_idempotency_key`, `broadcaster::tests::broadcast_error_display_names_the_kind_of_failure` |
| SR-FUND-015 | An outpoint the service has spent SHALL NOT be offered to another funding request while the chain still reports it as unspent. A refresh replaces the cache with what the chain reports, and a transaction in the mempool is not yet reported, so the inputs a broadcast transaction spent SHALL be held out of the cache and the change it created SHALL be held in it, until the chain reports otherwise or the hold expires. Repeated identical requests SHALL therefore produce different transactions, each spending different inputs. | Must | **AT:** `client::tests::cs_426_repeated_requests_do_not_hand_out_the_same_outpoint`, `cs_426_a_refresh_does_not_return_an_input_already_spent`, `cs_426_change_survives_a_refresh_that_has_not_seen_it`, `cs_426_the_service_defers_to_the_chain_once_it_catches_up`, `cs_427_varying_the_amount_still_must_not_reuse_an_input`, `sr_fund_012_a_successful_broadcast_reserves_its_inputs` |
| SR-FUND-016 | A rejection naming a conflicting transaction SHALL be treated as final regardless of the upstream's `failureRetryable` flag, and reported as `broadcast_rejected`. A conflict is in the transaction's inputs, and once another transaction holds them, resubmitting the identical transaction cannot succeed; mapi-lite reports `txn-mempool-conflict` as retryable, which told callers to retry a double spend that never would. A rejection naming no conflict SHALL keep the upstream's own verdict. | Must | **AT:** `broadcaster::mapi::tests::cs_427_a_conflict_is_final_even_when_the_server_calls_it_retryable`, `cs_427_a_retryable_rejection_without_a_conflict_stays_retryable` |
| SR-FUND-017 | Funding limits SHALL be judged against what the client's UTXO set can actually pay out, not against a single-input fee estimate. The service SHALL distinguish two cases and report a figure the caller can act on in each: `insufficient_balance` when the amount exceeds what the balance could fund even held in one UTXO, and `no_suitable_utxo` when it exceeds what the current UTXO set can fund but not that ceiling, in which case the description SHALL state both limits. The reported maximum SHALL account for the fee each additional input adds, so an input costing more in fees than it contributes is not counted. Any request the check admits SHALL be buildable. | Must | **AT:** `client::tests::cs_422_max_fundable_accounts_for_the_fee_each_input_adds`, `cs_422_insufficient_balance_reports_what_can_actually_be_paid_out`, `cs_422_no_suitable_utxo_does_not_quote_a_cost_nobody_would_pay`, `cs_422_the_two_codes_split_at_the_point_consolidating_stops_helping`, `cs_422_the_reported_maximum_can_actually_be_funded`, `cs_422_anything_the_check_admits_can_be_built`, `cs_422_a_utxo_covering_the_cost_exactly_is_not_offered_as_fundable` |
| SR-FUND-018 | `GET /client/{client_id}/balance` SHALL report `max_fundable`: the largest amount a single `POST /fund` could request, assuming one standard P2PKH outpoint. A caller SHALL NOT have to derive it from the balance, which cannot be done without knowing the fee and how many inputs a transaction would spend. The reported value SHALL be accepted by `POST /fund`. | Must | **AT:** `rest_api::tests::cs_422_balance_reports_the_fundable_maximum`, `cs_422_the_advertised_maximum_is_accepted_by_fund` |
| SR-FUND-019 | Unconfirmed outputs SHALL be eligible for funding: the service SHALL draw on the whole unspent set rather than the confirmed part of it, and `max_fundable` SHALL count unconfirmed value. This SHALL be stated in the endpoint documentation, since a caller cannot tell from the response which outputs a funding transaction drew on. | Must | **AT:** `client::tests::cs_425_unconfirmed_outputs_are_spendable`, `system_requirements::tests::sr_fund_019_endpoints_document_unconfirmed_eligibility` |
| SR-FUND-020 | The reported balance SHALL be derived from the client's unspent set, not obtained from a separate query, so that `confirmed`, `unconfirmed` and `max_fundable` always describe the same coins. An output the service has just spent SHALL leave the balance immediately rather than at the next refresh, and an output the service creates but has not broadcast SHALL count as unconfirmed. | Must | **AT:** `client::tests::cs_425_balance_is_derived_from_the_unspent_set`, `cs_425_the_balance_follows_a_spend_without_a_refresh`, `cs_425_change_from_a_new_transaction_is_unconfirmed` |
| SR-FUND-021 | Cached chain state SHALL be reused rather than refetched while it is younger than `service.chain_state_max_age_seconds`, which SHALL default to `utxo_refresh_period` so that a request reuses what the periodic refresh last fetched and the steady-state cost is one request per client per period whatever the traffic. `0` SHALL refresh on every request. The periodic refresh SHALL likewise skip clients already refreshed within the window. | Must | **AT:** `service::tests::cs_431_requests_inside_the_window_reuse_the_cached_state`, `cs_431_a_zero_window_refreshes_on_every_request`, `cs_431_the_periodic_sweep_skips_clients_a_request_refreshed` |
| SR-FUND-022 | A broadcast the upstream refused, or whose outcome is unknown, SHALL mark the client's cached chain state stale rather than refetching it there and then, so the next request refreshes and a request is spent only when one is wanted. This bounds the cost of building on a stale view to a single attempt: a conflicting input is refused, and the attempt after it works from fresh state. | Must | **AT:** `service::tests::cs_431_a_refused_broadcast_makes_the_next_request_refresh`, `cs_431_a_failed_broadcast_does_not_refresh_on_the_spot` |
| SR-FUND-023 | The fee the service pays on a transaction it builds SHALL be `ceil(tx_bytes * satoshis_per_kb / 1000)`, where the rate is configured by `fees.satoshis_per_kb` rather than fixed in the code. Rounding SHALL be upwards, so that rounding never underpays. A rate of zero SHALL be refused at startup, since a transaction paying no fee is not relayed. | Must | **AT:** `client::tests::cs_451_the_fee_follows_the_configured_rate`, `cs_451_a_partial_satoshi_rounds_up_rather_than_down`, `cs_451_a_new_client_starts_at_the_default_rate`, `cs_451_an_input_worth_less_than_its_own_fee_is_left_out`, `config::tests::cs_451_a_zero_rate_is_refused_at_startup`, `cs_451_the_fees_section_is_optional` |
| SR-FUND-024 | When `[mapi_lite]` is configured and `fees.use_mapi_fee_quote` is set, the service SHALL take the fee rate from mapi-lite's fee quote, so one server sets the rate for every service broadcasting through it. The configured rate SHALL remain the fallback, used before the first quote and whenever a quote cannot be had or is unusable, so an unreachable mapi-lite stops the rate changing rather than stopping funding. The quote SHALL be refreshed on the periodic sweep rather than on the funding path. A client added at runtime SHALL take the rate in force. | Must | **AT:** `service::tests::cs_451_the_mapi_fee_quote_sets_the_rate`, `cs_451_a_failed_quote_leaves_the_rate_alone`, `cs_451_the_quote_can_be_turned_off`, `cs_451_a_client_added_later_gets_the_rate_in_force`, `client::tests::cs_451_a_zero_rate_is_refused_and_the_last_good_rate_stands` |
| SR-FUND-025 | Change too small to be worth an output SHALL NOT be paid back; it SHALL remain in the transaction as additional fee. The threshold SHALL be derived from the fee rate in force rather than configured, as the round-trip cost of a change output -- the bytes it adds to the transaction that creates it plus the bytes it costs to spend later -- so it cannot drift out of step with what the service is paying, and SHALL never be zero. `max_fundable` SHALL therefore hold nothing back for change, so funding the reported maximum spends the client out exactly rather than leaving an output that costs more to spend than it holds. | Must | **AT:** `client::tests::cs_452_funding_the_maximum_leaves_no_dust_output`, `cs_452_dust_change_goes_to_the_fee_rather_than_an_output`, `cs_452_a_utxo_covering_the_cost_exactly_is_fundable`, `cs_452_the_threshold_is_the_round_trip_cost_of_an_output`, `cs_452_the_formula_reproduces_bitcoin_cores_number`, `cs_452_the_threshold_follows_the_fee_rate`, `cs_452_the_threshold_is_never_zero` |
| SR-FUND-026 | Where more than one UTXO could fund a request, the service SHALL prefer one whose change is either nothing or worth an output, so that folding dust into the fee does not give value away on an ordinary spend. It SHALL fall back to a UTXO that leaves dust rather than refuse a request it could fund. The index reported for each funded outpoint SHALL be derived from the transaction, since a transaction with no change output starts its funded outputs at 0 rather than 1. | Must | **AT:** `client::tests::cs_452_a_utxo_that_would_leave_dust_is_not_preferred`, `rest_api::tests::cs_452_the_reported_index_follows_a_missing_change_output` |
| SR-FUND-027 | The inputs the service's funding transactions have spent, and the change they have created, SHALL survive a restart of the service while the chain has not yet caught up with them. They SHALL be written to disk after every funding commit, successful or of unknown outcome, and read back before the first chain refresh, so a restarted service neither rebuilds a transaction it has already sent nor offers its inputs to another request. Entries older than the reservation window SHALL be dropped rather than restored. A missing or unreadable file SHALL NOT prevent startup. | Must | **AT:** `service::tests::cs_465_a_restart_does_not_hand_out_an_input_already_spent`, `cs_465_a_restart_carries_on_from_its_own_change`, `cs_465_an_uncertain_broadcast_is_remembered_across_a_restart`, `cs_465_an_unreadable_state_file_does_not_stop_startup`, `client::tests::cs_465_in_flight_state_survives_a_round_trip`, `cs_465_an_entry_older_than_the_window_is_not_restored`, `cs_465_a_moment_in_the_future_is_held_rather_than_dropped`, `config::tests::cs_465_the_state_file_sits_beside_the_dynamic_config` |

## Functional requirements — client management

| ID | Requirement | Priority | Verification |
|----|-------------|----------|--------------|
| SR-CLNT-001 | The service SHALL support multiple clients from static `[[client]]` configuration. | Must | **AT:** `config::tests::sr_clnt_001_config_supports_multiple_static_clients`, `rest_api::tests::sr_clnt_001_service_supports_multiple_configured_clients` |
| SR-CLNT-002 | Adding a duplicate `client_id` SHALL return HTTP 409 with code `client_exists`. | Must | **AT:** `rest_api::tests::test_add_client_duplicate` |
| SR-CLNT-003 | Adding a client with an invalid WIF SHALL return HTTP 400 with code `invalid_request`. | Must | **AT:** `rest_api::tests::test_add_client_invalid_wif`, `client::tests::test_invalid_wif_key` |
| SR-CLNT-004 | `POST /client` SHALL accept `wif_env` and `api_key_env` and persist `env:VAR` references instead of secret values. | Must | **AT:** `dynamic_config::tests::sr_clnt_004_and_sr_nfr_008_add_client_persists_to_dynamic_config_file`, `rest_api::tests::test_add_client_with_wif_env` |

## Functional requirements — blockchain

| ID | Requirement | Priority | Verification |
|----|-------------|----------|--------------|
| SR-BCHN-001 | The service SHALL support blockchain backends `woc`, `uaas`, `rpc`, and `test` via configuration. | Must | **AT:** `blockchain_factory::tests::sr_bchn_001_blockchain_factory_supports_woc_test_uaas_and_rpc`, `sr_bchn_001_blockchain_factory_rejects_unknown_interface_type` |
| SR-BCHN-002 | The service SHALL support network types `mainnet`, `testnet`, `stn`, and `regtest`. | Must | **AT:** `config::tests::sr_bchn_002_get_network_supports_mainnet_testnet_stn_and_regtest` |
| SR-BCHN-003 | The service SHALL periodically refresh all client UTXO balances at the interval configured by `service.utxo_refresh_period` (seconds). | Must | **AT:** `config::tests::sr_bchn_003_sample_config_sets_utxo_refresh_period` |
| SR-BCHN-004 | `/health` SHALL be liveness only: it SHALL NOT depend on the blockchain (read) interface, on mapi-lite, or on any other upstream, so a probe wired to it can only ever report on this process. Upstream reachability belongs to `/ready` (SR-BCHN-011, SR-BCHN-012). | Must | **AT:** `rest_api::tests::test_health`, `sr_bchn_012_health_stays_up_when_mapi_lite_is_unreachable`, `service::tests::sr_bchn_011_mapi_lite_health_is_none_without_mapi_lite` |
| SR-BCHN-005 | The `rpc` interface SHALL require the node's address, username and password, and SHALL name the missing one at startup. | Must | **AT:** `blockchain_factory::tests::sr_bchn_005_rpc_interface_requires_address_and_credentials`, `config::tests::sr_bchn_005_rpc_config_requires_address_and_credentials` |
| SR-BCHN-006 | The service SHALL be configurable against a regtest chain via the `rpc` interface. | Must | **AT:** `blockchain_factory::tests::sr_bchn_006_rpc_backend_can_be_built_for_regtest` |
| SR-BCHN-007 | Only backends that index nothing themselves SHALL be sent address imports, and `rpc_import_addresses = false` SHALL disable them. | Must | **AT:** `blockchain_factory::tests::sr_bchn_007_only_the_rpc_backend_supplies_an_address_watcher`, `sr_bchn_007_rpc_import_addresses_false_supplies_no_watcher`, `service::tests::sr_bchn_007_no_watcher_means_no_import_attempts` |
| SR-BCHN-008 | When address imports are enabled, the service SHALL ask the backend to watch each configured client's address at startup and each client added at runtime, and SHALL continue running if an import fails. | Must | **AT:** `service::tests::sr_bchn_008_configured_client_addresses_are_watched_at_startup`, `sr_bchn_008_a_runtime_added_client_address_is_watched`, `sr_bchn_008_a_failed_import_does_not_prevent_startup` |
| SR-BCHN-009 | Transaction broadcasting SHALL sit behind a `TxBroadcaster` seam with two implementations. When `[mapi_lite]` is configured, funding transactions SHALL be broadcast via mapi-lite (`POST /mapi/txs`) and `GET /status` SHALL report `broadcaster: "mapi-lite"`; otherwise they SHALL be broadcast via the configured blockchain interface, unchanged from before. Chain reads SHALL use the blockchain interface in both cases. A submit SHALL be refused when mapi-lite reports a txid other than the transaction's own, since the caller's outpoints are derived from the local hash, and SHALL be bounded as a whole by `total_timeout_seconds` across its retries. | Must | **AT:** `broadcaster::mapi::tests::mapi_broadcaster_refuses_a_txid_that_is_not_the_transactions_own`, `mapi_broadcaster_submit_is_bounded_by_the_total_deadline`, `broadcaster::factory::tests::sr_bchn_009_factory_selects_the_blockchain_interface_without_mapi_lite`, `sr_bchn_009_factory_selects_mapi_lite_when_configured`, `broadcaster::mapi::tests::sr_bchn_009_mapi_broadcaster_posts_the_transaction_to_mapi_txs`, `service::tests::sr_bchn_009_execute_funding_uses_the_broadcaster_not_the_blockchain_interface`, `sr_bchn_009_fund_with_multiple_transactions_uses_the_broadcaster`, `rest_api::tests::sr_bchn_009_status_names_mapi_lite_and_fund_broadcasts_through_it`, `system_requirements::tests::sr_bchn_009_uls_client_and_uls_core_are_pinned_to_one_mapi_lite_revision`, `sr_bchn_009_configuration_and_readme_document_mapi_lite` |
| SR-BCHN-010 | At startup the service SHALL log at INFO which broadcaster is in use; when mapi-lite is configured the line SHALL say so and include its base URL, and SHALL NOT include the token. | Must | **AT:** `broadcaster::factory::tests::sr_bchn_010_describe_broadcaster_names_mapi_lite_and_its_url_when_configured`, `sr_bchn_010_describe_broadcaster_names_the_blockchain_interface_otherwise`, `describe_broadcaster_never_includes_the_token`, `system_requirements::tests::sr_bchn_010_main_logs_which_broadcaster_is_in_use` |
| SR-BCHN-011 | When `[mapi_lite]` is configured, `GET /ready` SHALL probe mapi-lite (`GET /mapi/feeQuote`, bounded by `health_timeout_seconds`) and SHALL return HTTP 503 with `status: "unhealthy"` when the probe fails, so an instance whose only broadcast path is unreachable is taken out of rotation rather than handed requests it will refuse. The verdict SHALL be cached for `health_timeout_seconds`, so this unauthenticated and rate-limit-exempt endpoint (SR-SEC-013) cannot be used to flood mapi-lite, and the 503 body SHALL carry a generic detail rather than the upstream error. An unreachable mapi-lite at startup SHALL be warned about but SHALL NOT stop the service, so the read paths stay up and the service recovers when mapi-lite returns. | Must | **AT:** `rest_api::tests::sr_bchn_011_ready_reports_mapi_lite_ok_when_it_is_reachable`, `sr_bchn_011_ready_returns_503_when_mapi_lite_is_unreachable`, `service::tests::sr_bchn_011_mapi_lite_health_reports_the_probe_result`, `sr_bchn_011_mapi_lite_health_is_cached_for_the_health_timeout`, `broadcaster::mapi::tests::sr_bchn_011_mapi_broadcaster_health_check_passes_on_a_fee_quote`, `sr_bchn_011_mapi_broadcaster_health_check_fails_on_an_error_status` |
| SR-BCHN-012 | The service SHALL expose `GET /ready` as a readiness probe, distinct from the `/health` liveness probe (SR-BCHN-004). Without `[mapi_lite]` it SHALL answer exactly as `/health` does, there being no upstream whose absence would stop funding. A failing upstream SHALL affect `/ready` only: `/health` SHALL stay HTTP 200 under the same configuration, because restarting the process cannot fix an upstream and a restart loop would take the read paths down with it. | Must | **AT:** `rest_api::tests::sr_bchn_012_ready_matches_health_without_mapi_lite`, `sr_bchn_012_health_stays_up_when_mapi_lite_is_unreachable`, `sr_bchn_011_ready_returns_503_when_mapi_lite_is_unreachable` |
| SR-BCHN-013 | The service SHALL bound the rate of its own requests to the blockchain interface, so it stays inside the interface's published limit rather than relying on the interface to refuse it. The limit SHALL apply across the whole service, so concurrent refreshes share one allowance. For `interface_type = "woc"` it SHALL default to 3 requests per second, which is what WhatsOnChain documents as free; for `rpc` and `uaas`, the operator's own servers, there SHALL be no default limit. An explicit `max_requests_per_second` SHALL override either default, and `0` SHALL disable the limit. For `woc` the limit SHALL bound **HTTP requests**, applied by the interface itself, since one interface call became many requests in chain-gang 0.11.3 and a decorator around the interface cannot see them; for the interfaces that offer no such control it SHALL bound interface calls, and the service SHALL NOT apply both to one interface. The chosen limit SHALL be logged at startup. | Must | **AT:** `outbound_rate_limit::tests::cs_418_a_burst_is_spaced_out_rather_than_sent_at_once`, `cs_418_concurrent_callers_share_the_allowance`, `config::tests::cs_418_only_the_public_api_is_rate_limited_by_default`, `cs_418_an_explicit_limit_overrides_the_default_either_way`, `blockchain_factory::tests::cs_457_only_woc_paces_its_own_requests`, `cs_457_woc_is_limited_by_default_and_the_backend_still_builds`, `cs_457_an_explicit_zero_leaves_the_interface_unlimited` |
| SR-BCHN-014 | A failure reaching the blockchain interface SHALL be logged with the length of the current run of failures, and a return to service SHALL be logged once, naming how long that run was. Warnings that simply stop leave a recovered service indistinguishable from a stuck one. | Must | **AT:** `service::tests::cs_418_recovery_is_announced_once_after_a_run_of_failures` |

## Security requirements

| ID | Requirement | Priority | Verification |
|----|-------------|----------|--------------|
| SR-SEC-001 | When a client `api_key` is configured, `POST /fund`, `GET /client/{id}/balance`, `GET /client/{id}/address`, and `DELETE /client/{id}` SHALL require matching credentials via `Authorization: Bearer` or `X-API-Key`. | Must | **AT:** `rest_api::tests::test_fund_requires_api_key_when_enabled`, `test_balance_requires_client_api_key_when_enabled`, `sr_sec_001_address_and_delete_require_client_api_key_when_enabled`, `auth::tests::authorize_client_*` |
| SR-SEC-002 | Missing or invalid client credentials SHALL return HTTP 401 with `{"code":"unauthorized","description":"Unauthorized"}`. | Must | **AT:** `rest_api::tests::sr_sec_002_unauthorized_response_uses_standard_json_body`, `auth::tests::authorize_client_rejects_missing_key` |
| SR-SEC-003 | A client's API key SHALL NOT authorize access to another client's endpoints. | Must | **AT:** `rest_api::tests::test_fund_rejects_wrong_client_api_key`, `auth::tests::authorize_client_rejects_wrong_client_key` |
| SR-SEC-004 | When `web_interface.admin_api_key` is configured, `POST /client` SHALL require the admin key. | Must | **AT:** `rest_api::tests::test_add_client_requires_admin_key_when_enabled`, `test_add_client_accepts_admin_bearer_token`, `test_add_client_rejects_wrong_admin_key`, `auth::tests::authorize_admin_*` |
| SR-SEC-005 | API key comparison SHALL use constant-time equality. | Must | **AT:** `auth::unit_tests::constant_time_eq_*` |
| SR-SEC-006 | WIF keys, client API keys, and admin API keys SHALL be loadable via `env:VAR_NAME` references at startup. | Must | **AT:** `secrets::tests::resolve_secret_reads_environment_variable`, `config::tests::resolve_secrets_replaces_env_references` |
| SR-SEC-007 | Missing environment variables referenced by `env:VAR` SHALL cause startup failure with a clear error. | Must | **AT:** `secrets::tests::resolve_secret_errors_when_env_var_missing`, `config::tests::sr_nfr_007_load_config_returns_error_for_invalid_file` |
| SR-SEC-008 | Environment overrides `FS_ADMIN_API_KEY`, `FS_CLIENT_{ID}_WIF`, and `FS_CLIENT_{ID}_API_KEY` SHALL take precedence over config file values. | Must | **AT:** `config::tests::resolve_secrets_applies_client_env_overrides`, `sr_sec_008_resolve_secrets_applies_client_api_key_env_override` |
| SR-SEC-009 | Plaintext secrets in configuration SHALL log a warning at startup (WIF, client `api_key`, `admin_api_key`). | Must | **AT:** `secrets::tests::sr_sec_009_plaintext_secret_fields_detects_literal_secrets` |
| SR-SEC-010 | Error responses SHALL NOT echo WIF or API key values. | Must | **AT:** `rest_api::tests::sr_sec_010_invalid_wif_error_does_not_echo_secret` |
| SR-SEC-011 | `/`, `/health`, `/ready`, and `/status` SHALL remain unauthenticated regardless of client API key configuration. | Must | **AT:** `rest_api::tests::test_health_unauthenticated_when_api_key_enabled`, `test_ready_unauthenticated_when_api_key_enabled`, `test_status_unauthenticated_when_client_api_key_enabled`, `sr_sec_011_index_unauthenticated_when_client_api_key_enabled` |
| SR-SEC-012 | When rate limiting is enabled, excess requests from the same IP SHALL receive HTTP 429 with a JSON error body. | Must | **AT:** `rest_api::tests::test_rate_limit_returns_429_when_burst_exceeded` |
| SR-SEC-013 | `/health` and `/ready` SHALL be exempt from rate limiting when rate limiting is enabled, so a throttled probe cannot report a healthy service as down. | Must | **AT:** `rest_api::tests::test_health_is_exempt_from_rate_limit`, `test_ready_is_exempt_from_rate_limit` |
| SR-SEC-014 | The node RPC password SHALL be resolvable from an `env:VAR` reference, SHALL be overridable by `FS_RPC_PASSWORD`, and SHALL be reported when stored as plaintext. | Must | **AT:** `config::tests::sr_sec_014_rpc_password_resolves_from_an_env_reference`, `sr_sec_014_fs_rpc_password_env_overrides_the_config`, `sr_sec_014_plaintext_rpc_password_is_reported` |
| SR-SEC-015 | The mapi-lite auth token SHALL be resolvable from an `env:VAR` reference, SHALL be overridable by `FS_MAPI_LITE_AUTH_TOKEN`, SHALL fail startup when a referenced variable is missing, and SHALL be reported when stored as plaintext. | Must | **AT:** `config::tests::sr_sec_015_mapi_lite_auth_token_resolves_from_an_env_reference`, `sr_sec_015_fs_mapi_lite_auth_token_env_overrides_the_config`, `sr_sec_015_missing_mapi_lite_auth_token_env_reference_fails_resolution`, `sr_sec_015_plaintext_mapi_lite_auth_token_is_reported` |

## Configuration and deployment requirements

| ID | Requirement | Priority | Verification |
|----|-------------|----------|--------------|
| SR-CFG-001 | The service SHALL load configuration from `data/financing-service.toml` by default. | Must | **AT:** `config::tests::sr_cfg_001_load_config_reads_toml_file` |
| SR-CFG-002 | The service SHALL accept configuration via the `FS_CONFIG` environment variable (JSON-encoded). | Must | **AT:** `config::tests::sr_cfg_002_get_config_reads_fs_config_json` |
| SR-CFG-003 | When `APP_ENV=docker`, the service SHALL listen on `0.0.0.0` regardless of configured `web_interface.address`. | Must | **AT:** `config::tests::sr_cfg_003_web_bind_address_uses_all_interfaces_in_docker` |
| SR-CFG-004 | Rate limiting configuration with `enabled = true` and invalid `requests_per_second` (zero) SHALL fail validation at startup. | Must | **AT:** `config::tests::rate_limit_config_rejects_zero_requests_per_second_when_enabled` |
| SR-CFG-005 | The Docker image SHALL include a health check against `GET /health`, the liveness endpoint, so a failing upstream cannot make Docker restart a working container. | Must | **AT:** `system_requirements::tests::sr_cfg_005_dockerfile_healthcheck_calls_health_endpoint` |
| SR-CFG-006 | Logging level SHALL be configurable via `[logging].level`. | Must | **AT:** `config::tests::sr_cfg_006_get_log_level_accepts_configured_levels` |
| SR-CFG-007 | Telemetry configuration SHALL be validated at startup. | Must | **AT:** `config::tests::sr_cfg_007_load_config_validates_telemetry_config` |
| SR-CFG-008 | The `[mapi_lite]` section SHALL be optional; a config without it SHALL load as before. When present, `base_url` SHALL be required and validated (`http://` or `https://`), timeouts SHALL be non-zero, `timeout_seconds`/`health_timeout_seconds`/`max_retries`/`total_timeout_seconds` SHALL default to 13/2/2/45, and the section SHALL also be accepted inside `FS_CONFIG` JSON. `base_url` SHALL be normalised (surrounding whitespace and trailing `/` stripped) before it is validated or used, so validation and the HTTP client read the same string. `health_timeout_seconds` SHALL be under 3 seconds, the timeout a conventional readiness probe allows `/ready`, and `total_timeout_seconds` SHALL be at least `timeout_seconds`. | Must | **AT:** `config::tests::sr_cfg_008_config_without_a_mapi_lite_section_has_none`, `sr_cfg_008_mapi_lite_section_parses_with_defaults`, `sr_cfg_008_mapi_lite_section_accepts_every_field`, `sr_cfg_008_mapi_lite_base_url_is_normalised_before_use`, `sr_cfg_008_mapi_lite_validate_rejects_a_missing_or_non_http_base_url`, `sr_cfg_008_mapi_lite_validate_rejects_zero_timeouts`, `sr_cfg_008_mapi_lite_validate_rejects_a_health_timeout_a_probe_cannot_wait_for`, `sr_cfg_008_mapi_lite_validate_rejects_a_total_timeout_below_one_attempt`, `sr_cfg_008_load_config_validates_the_mapi_lite_section`, `sr_cfg_008_load_config_reads_a_valid_mapi_lite_section`, `sr_cfg_008_fs_config_json_accepts_a_mapi_lite_object`, `broadcaster::mapi::tests::mapi_broadcaster_tolerates_a_trailing_slash_on_the_base_url` |
| SR-CFG-009 | The `[mapi_lite]` defaults SHALL be a set that can all hold at once: `timeout_seconds` x (`max_retries` + 1) plus back-off SHALL fit inside `total_timeout_seconds`, so the configured retry count is the count that runs. Where an operator's own combination cannot fit, the service SHALL log a warning at startup naming how many attempts will actually start, and SHALL still start -- the combination is legal, it simply does fewer attempts than it asks for. | Must | **AT:** `config::tests::sr_cfg_009_the_default_retry_budget_fits_the_default_deadline`, `sr_cfg_009_a_budget_that_cannot_fit_is_warned_about`, `sr_cfg_009_raising_max_retries_alone_is_warned_about` |

## Observability requirements — OpenTelemetry

| ID | Requirement | Priority | Verification |
|----|-------------|----------|--------------|
| SR-TELE-001 | OpenTelemetry trace export via OTLP SHALL be optional and disabled by default. | Must | **AT:** `telemetry::tests::telemetry_defaults_to_disabled_without_otel_exporter_env` |
| SR-TELE-002 | When telemetry export is enabled, the service SHALL create a trace span per HTTP request. | Must | **AT:** `system_requirements::tests::sr_tele_002_main_wraps_tracing_logger` |
| SR-TELE-003 | Telemetry SHALL be configurable via `[telemetry]` and standard `OTEL_*` environment variables. | Must | **AT:** `telemetry::tests::otel_traces_exporter_env_enables_telemetry`, `effective_service_name_prefers_otel_service_name_env`, `effective_otlp_endpoint_uses_standard_env_vars` |
| SR-TELE-004 | Enabled export SHALL attach `service.name` and `service.version` resource attributes to traces. | Must | **AT:** `system_requirements::tests::sr_tele_004_telemetry_sets_service_resource_attributes` |
| SR-TELE-005 | Telemetry behaviour SHALL be documented in Configuration.md and README.md. | Must | **AT:** `system_requirements::tests::sr_tele_005_readme_and_configuration_document_opentelemetry` |

## Non-functional requirements

| ID | Requirement | Priority | Verification |
|----|-------------|----------|--------------|
| SR-NFR-001 | The codebase SHALL pass `cargo fmt --check` without formatting violations. | Must | **AT:** `system_requirements::tests::sr_nfr_001_ci_runs_cargo_fmt_check` |
| SR-NFR-002 | The codebase SHALL pass `cargo clippy -- -D warnings` without warnings. | Must | **AT:** `system_requirements::tests::sr_nfr_002_ci_runs_cargo_clippy_with_warnings_denied` |
| SR-NFR-003 | Dependencies SHALL pass `cargo audit` without known vulnerabilities (or with documented exceptions). | Must | **AT:** `system_requirements::tests::sr_nfr_003_ci_runs_cargo_audit` |
| SR-NFR-004 | All automated tests SHALL pass on supported platforms in CI. | Must | **AT:** `system_requirements::tests::sr_nfr_004_ci_runs_cargo_test` |
| SR-NFR-005 | `Cargo.lock` SHALL be committed for reproducible builds. | Must | **AT:** `system_requirements::tests::sr_nfr_005_cargo_lock_is_committed` |
| SR-NFR-007 | Startup configuration or secret resolution errors SHALL return an error from `main` rather than panic. | Must | **AT:** `system_requirements::tests::sr_nfr_007_main_returns_error_instead_of_panicking_on_config_failure`, `config::tests::sr_nfr_007_load_config_returns_error_for_invalid_file` |
| SR-NFR-008 | The service SHALL maintain an in-memory UTXO cache per client and persist runtime-added clients to the dynamic config file. | Must | **AT:** `dynamic_config::tests::sr_clnt_004_and_sr_nfr_008_add_client_persists_to_dynamic_config_file`, `sr_nfr_008_remove_client_updates_dynamic_config_file`, `client::tests::sr_fund_010_plan_funding_tx_leaves_utxo_cache_unchanged_until_commit` |
| SR-NFR-009 | The `uls-client`/`uls-core` dependencies SHALL be git+SSH pins to one mapi-lite revision, fetched by the system git (`.cargo/config.toml`), with the deploy key loaded in CI and the SSH agent forwarded into the Docker build; `chain-gang` and `reqwest` SHALL each resolve to exactly one package in `Cargo.lock`. | Must | **AT:** `system_requirements::tests::sr_nfr_009_cargo_fetches_git_dependencies_with_the_system_git`, `sr_nfr_009_ci_loads_the_mapi_lite_deploy_key_before_building`, `sr_nfr_009_docker_build_mounts_ssh_for_the_dependency_fetch`, `dependency_graph::tests::chain_gang_resolves_to_exactly_one_package`, `reqwest_resolves_to_exactly_one_package`, `the_two_uls_crates_come_from_one_mapi_lite_revision` |

## Constraints and known limitations

| ID | Constraint | Verification |
|----|------------|--------------|
| SR-LIM-001 | HD (BIP-32) wallets are not supported. | **AT:** `system_requirements::tests::sr_lim_001_hd_wallet_support_is_not_implemented` |
| SR-LIM-002 | HTTPS is not provided by the service; TLS is a deployment responsibility. | **AT:** `system_requirements::tests::sr_lim_002_https_is_not_provided_by_the_service_binary` |
| SR-LIM-003 | On-chain transactions from partial `multiple_tx` failure cannot be rolled back. | **AT:** `service::tests::partial_broadcast_error_lists_successful_txids` |
| SR-LIM-004 | Rate limiting behind a reverse proxy applies to the proxy IP unless a custom key extractor is implemented. | **AT:** `rate_limit::tests::sr_lim_004_rate_limit_uses_peer_ip_key_extractor` |
| SR-LIM-005 | Same-client concurrent funding may contend for the same UTXO; one request may fail and trigger resync. | **AT:** `system_requirements::tests::sr_lim_005_same_client_concurrency_is_covered_by_service_tests`, `service::tests::concurrent_fund_requests_for_same_client_do_not_block` |
| SR-LIM-006 | OpenTelemetry export is traces only; metrics and OTLP log export are not supported. | **AT:** `system_requirements::tests::sr_lim_006_opentelemetry_exports_traces_only` |

## Verification summary

Run the full automated verification suite:

```bash
cargo fmt --all -- --check
cargo clippy -- -D warnings
cargo audit
cargo test
```

These commands mirror the GitHub Actions workflow (`.github/workflows/rust.yml`). The `cargo test` step executes all requirement tests above (currently **114** tests).

## Related documentation

* [SupportedEndpoints.md](SupportedEndpoints.md) — REST API reference
* [Configuration.md](Configuration.md) — service and client configuration
* [Project.md](Project.md) — architecture and implementation status
* [Development.md](Development.md) — build, test, and CI
* [README.md](../README.md) — overview and getting started
